pipeline {
    agent any

    stages {
        stage('Build docker image. Build app.') {
            steps {
                git 'git@github.com:vdmemory/dockerized-react-test.git'
                sh 'docker-compose build --no-cache && docker-compose up -d'
            }
        }
    }
}